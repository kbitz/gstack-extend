# Changelog

All notable changes to {{name}}.

Format roughly follows [Keep a Changelog](https://keepachangelog.com/). Project uses a 4-digit version scheme (`MAJOR.MINOR.PATCH.MICRO`); source of truth is `VERSION`.

## [0.0.0.0] - {{created_at_date}}

### Added
- Initial project scaffold via `gstack-extend init`.
