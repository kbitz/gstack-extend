# TODOS

## Unprocessed

- [ship] **Track 5A follow-up: bin/update-run upgrade-flow integration test** -- CP#3 sub-bullet 5 wasn't implemented in v0.18.14. The test would trigger `bin/update-run` and verify post-upgrade skills still resolve via path-1. Low priority because (a) `bin/update-run` is unchanged in this Track, (b) post-upgrade resolution goes through path-1 which IS exercised, (c) update-run has its own non-Track-5A test coverage. P2. ~20 LoC + git-fetch fixture. _[tests/update.test.ts]._

- [ship] **Track 5A follow-up: doc-type math unit tests** -- The `_inferred_doc_type` math is exercised end-to-end via `tests/roadmap-audit/doc-type-mismatch/` but isolated unit tests for the boundary cases (empty file, exactly 4 content lines, exactly 5 content lines with 25% density vs 60% density) are not in `tests/checks-doc-type.test.ts`. P3. ~30 LoC. _[tests/checks-doc-type.test.ts]._

- [ceo-review] **Project Bootstrapping Group** — homes for two related items deferred from Track 5A (D9 cascade): (a) `bin/roadmap-audit init` subcommand that creates `docs/`, `docs/designs/`, `docs/archive/` in a fresh project and offers to git-mv misplaced docs (consumes both `check_doc_location` and Track 5A's new `check_doc_type` findings); on destination collisions, AskUserQuestion with diff/merge/skip/abort. ~100 LoC + tests. (S-M effort). (b) `gstack-extend init <project>` 10x-version: full bootstrap with starter ROADMAP.md (Phase block + Track template), CLAUDE.md scaffold, project registration in `~/.gstack-extend/projects.yaml`, and initial `/roadmap` audit. Depends on (a). M-L effort. Sequenced after Track 5A ships (depends on doc-type heuristic from this Track). _Why deferred: Track 5A is named "Install pipeline polish" but this work is project bootstrapping; codex outside-voice review confirmed scope creep concern. CEO plan: ~/.gstack/projects/kbitz-gstack-extend/ceo-plans/2026-05-06-track-5a-install-pipeline-polish.md._
