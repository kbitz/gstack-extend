# Progress

## Phase 2: Gstack parity (current)

Graft gstack's consistency patterns into extend's three daily-use skills (pair-review, roadmap, full-review) so workflows feel as reliable as gstack core.

| Version | Date | Summary |
|---------|------|---------|
| 0.18.13.0 | 2026-05-05 | Auto-create GitHub Releases on `VERSION` change. The existing `auto-tag.yml` workflow already pushed an annotated tag whenever `VERSION` changed on `main`, but no `gh release create` ever ran — 53 tags accumulated on the repo with zero entries on the Releases page. The workflow now extracts the matching `## [X.Y.Z]` section from `CHANGELOG.md` (via the new `scripts/extract-changelog-section.sh`, reusable from local scripts and CI) and creates a GitHub Release with those notes, falling back to gh's `--generate-notes` plus a `::warning::` if the section is missing. The release step is idempotent (`gh release view "$TAG"` short-circuits on re-runs), composes cleanly with the tag step's existing idempotency, and runs under the workflow's existing `contents: write` permission. Backfill: all 53 historical tags (`v0.1.1` → `v0.18.12.1`) now have releases, with `v0.18.12.1` marked Latest; the two pre-CHANGELOG tags got a placeholder note. The extractor handles the trailing-`.0` mismatch where 4-digit tags like `v0.18.5.0` map to 3-digit CHANGELOG entries like `## [0.18.5]`. |
| 0.18.12.0 | 2026-05-04 | Install enforces bun. `setup` now fails fast with checksum-verified install instructions if `bun` is not on PATH — mirrors gstack's pattern (`gstack/setup` lines 5-14). Bun was already required transitively (`bun run test`, `scripts/select-tests.ts`, the `src/audit/cli.ts` shim from Track 2B), so this just makes the existing requirement explicit at install time and gives users a clear error instead of confusing downstream "command not found" failures. New `bun run setup` script alias in `package.json` so the install path also works through the bun CLI verbatim — `bun --cwd ~/.claude/skills/gstack-extend run setup` is now equivalent to the direct `./setup` invocation. README Installation section lists Bun v1.0+ as a requirement upfront alongside Claude Code and Git. Stale roadmap reference fixed: per-project install support points to `Group 5 Pre-flight 2 and Track 5A` (was `Group 1 Pre-flight 2 and Track 1A`, which referred to an older roadmap structure pre-restructure). 25-line diff (3 source files + CHANGELOG + VERSION); existing `tests/update.test.ts` covers the bun-present setup path; bun-missing path manually verified with `PATH=/usr/bin:/bin ./setup`. |
| 0.18.11.0 | 2026-05-04 | Group 2 Track 2B + Group 4 Track 4C land together (Track 4D took the v0.18.10.0 slot in PR #62). **Track 2B (cutover):** `bin/roadmap-audit` is now a 7-line POSIX-sh shim that runs `src/audit/cli.ts` via `bun`. Track 2A (v0.18.6.0) shipped the TS port with full byte-parity verified by `tests/audit-shadow.test.ts` but kept it as "dark code" — the binary was never replaced, so the snapshot suite continued running bash and the shadow parity test paid bash cost a second time per fixture. This cutover makes TS the single source of truth: `tests/audit-shadow.test.ts` deleted (parity check obsolete when runner and oracle are the same code), and `tests/{audit-snapshots,audit-cli-contract}.test.ts` gain `src/audit/**` in their manual touchfile entries so audit code edits retrigger the snapshot suite under diff selection. Measured: `audit-snapshots` 111s → 7.3s (~15×), full suite 113s → 32s (~3.5×). Phase 1's stated end-state ("`bin/roadmap-audit` is a compiled bun binary") is met — shim, not `bun build --compile` artifact, but equivalent semantics with simpler deploy and grep-able source. The bash binary (3,868 lines) is preserved in git history at e4f883b for archaeology. Next leverage point is `tests/update.test.ts` (~11s) which shells out to `bin/update-{check,run}` and the bash `setup` script — Group 5 scope, not Group 2. **Track 4C (skill prose corpus + in-session routing rule):** New `tests/fixtures/skill-prose-corpus/` ships 4 reference fixtures with rich YAML-frontmatter provenance — 3 positive examples representing what good `/roadmap` reassessment, `/test-plan` extraction, and `/pair-review` test-list output looks like, plus a `4-shallow-control` negative example (deliberately vague and generic). Each fixture records source skill commit, repo commit at capture, input prompt, generation model, UTC timestamp, worktree state. The corpus is **reference material, not a test fixture** — there is no automated judge running under `bun test`. The eng-review's original SDK + `EVALS=1` + `ANTHROPIC_API_KEY` pattern was reconsidered during /ship and dropped: the test only runs on machines that already have `claude` authenticated (it never runs in CI), so requiring a separate API key plus a separate Anthropic SDK dependency was setup friction with no ecological payoff. Instead, `CLAUDE.md ## Testing` adds a routing rule: when `skills/*.md` is edited in a session, Claude proactively recommends judging the changed prose against the corpus and the three-axis rubric (clarity / completeness / actionability, 1–5 each, ≥3 expected on positives, ≤2 expected on at least one axis for the control) in-session. Cost is whatever the existing Claude Code session is paying anyway with prompt caching across fixtures; cadence is opportunistic (only on real prose changes), not scheduled. The two follow-ups the eng-review deferred to `Future` (tool-use migration, judge-floor tightening 3 → 4) are obsolete in this shape — there's no judge code to migrate and no scheduled run to retune. `node_modules/` added to `.gitignore` (was leaking during /ship cycles). |
| 0.18.8.1 | 2026-05-03 | Group 4 implementer artifacts. New `docs/designs/group-4/` directory with locked plan files extracted from the three Track eng-review sessions (4A from bogota, 4C from valletta, 4D from richmond). Each `track-4X-plan.md` captures the full final scope, all locked decisions, codex catches batch-applied, cross-model tensions resolved, and failure modes — implementers ship without re-running `/plan-eng-review`. New workspaces bootstrap by reading three files: `docs/designs/group-4-replan.md` (re-plan context), `docs/ROADMAP.md` Track 4X entry (summary), `docs/designs/group-4/track-4X-plan.md` (full plan). README.md documents the workflow. Plans get deleted as each Track ships; directory disappears when Group 4 closes. Portable across machines and Conductor resets — replaces the local-only `~/.claude/projects/.../session.jsonl` extraction path. |
| 0.18.8.0 | 2026-05-03 | Group 4 re-plan. New design doc `docs/designs/group-4-replan.md` (295 lines) audits the four parallel `/plan-eng-review` plans (Tracks 4A/4B/4C/4D, run in bogota/dalat-v1/valletta/richmond workspaces 2026-05-03) and eliminates the defensive workarounds the parallel-review topology produced. Findings: 4B's "NOT parallel-safe with 4C on `eval-store.ts`" was phantom (4C never edits `eval-store.ts`); 4B's proposed deferred-reader "Track 4D" name-collided with the existing audit-compliance Track 4D; 4A's TEST_TIERS deferral was unnecessary because 4C self-gates on `EVALS=1`; 4B's narrow scope was "scaffolding pretending to be a gate" because no Track in the codebase produces eval-store data. **Resolution:** Track 4B dropped from Group 4 to `Future` (the full original eval persistence + reader + regression gate scope, deferred until a producer Track exists). New Pre-flight `4A-audit` gate added (timing audit, no code, ~30 min) — kill-cheap option for Track 4A's L scope; greenlight ≥40% wall-clock saved on the median of 3 recent PRs against the measured 117s baseline, kill <25%. Track 4A bumped M → L (~150 → ~630 LOC) per the codex hybrid-import-graph approach: TS AST walk + manual map + 4 safety fallbacks + argv passthrough + signal forwarding + `TOUCHFILES_BASE` env + `--name-status` rename safety + 3 structural invariants + inlined `makeEmptyRepo` hardening. Track 4C bumped ~250 → ~370 LOC: locked callJudge with baked-in validator, `maxRetries:0`, stop_reason guard, isJudgeScore strict predicate, sequential `test.each`, `EVALS === '1'` exact gate, 60s per-test timeout, 3+1 captured-prose fixtures with rich provenance. Track 4D unchanged in scope (3 describes + `REGISTERED_SOURCES` export + grammar-list fix + TODO retag). Track 4C declares `_Depends on: Track 4A_` so the audit's COLLISIONS check passes on the additive `package.json` + `CLAUDE.md ## Testing` merge overlap — serializes the merge order, not the work; drop the dep if Pre-flight kills 4A. `Future` gains 7 items: full Track 4B scope (writer + reader + regression), gbrain-sync allowlist for evals dir, eval dir retention/pruning policy, audit fail-taxonomy calibration, SKILLS-list dedup helper extraction, callJudge tool-use migration (trigger: 2nd consumer or regex bug), judge-floor tightening 3 → 4 after 5–10 EVALS runs. Group 4 reshapes from 4 tracks to 3 tracks + 1 Pre-flight gate; 13 total tasks unchanged. The four existing eng-review session plans stay valid as implementation input — implementers resume each Conductor session and "implement what we planned"; no re-review needed. Mechanics for execution (Phase 0 cleanup → Phase 1 audit → Phase 2 TODOS triage → Phase 3 parallel implement → Phase 4 ship) documented in the design doc. |
| 0.18.7.0 | 2026-04-30 | Group 3, Track 3A: migrate test runners + invariants test. Replace 7 bash test runners (~2,800 LOC) with bun:test files (~1,400 LOC TS), add a structural-invariants safety net (`tests/audit-invariants.test.ts`), and pin the audit's CLI contract beyond stdout (`tests/audit-cli-contract.test.ts`). Headline win is maintainability — bash is the wrong language for markdown-shaped state-machine tests, and the TS port lets parsers + helpers + scorer be unit-tested directly. `/ship` test runtime drops from ~50s to ~30s; the full ~10s arrives only when Track 2A's compile-binary cutover replaces the bash `bin/roadmap-audit`. New: `tests/helpers/{fixture-repo.ts, run-bin.ts}` (consolidated mkdtemp + git init + spawn-env scoping; `runBin()` requires explicit `home` to prevent concurrent test cross-pollution); `src/audit/sections.ts` (canonical section spec, side-effect-free so importing it from invariants tests can't pull argv parsing on module load); `src/test-plan/parsers.ts` (two awk pipelines from the bash e2e test lifted to pure functions: `parseGroupTracks` and `scanPairReviewSession`, each with 8+ ugly-input unit tests covering Unicode, completion suffixes, sub-track IDs, branch mismatch); `scripts/score-extractor.ts` (extracts `--score` developer harness from the bash extractor test; documented exit codes 0/1/2, --help, 15-test unit suite, drops the python3 dependency); `tests/fixtures/extractor-corpus/` (vendors the two design docs the bash test referenced as `$HOME`-relative, kb-only paths; each has a provenance comment block); `scripts/verify-migration-parity.sh` (one-shot PR gate: informational count check + BLOCKING named-scenario check, retired post-merge as dead code). 6 migrated test files: `tests/audit-snapshots.test.ts` (was `test-roadmap-audit.sh`), `tests/skill-protocols.test.ts`, `tests/test-plan.test.ts`, `tests/test-plan-extractor.test.ts`, `tests/test-plan-e2e.test.ts`, `tests/update.test.ts`. Plus `scripts/test-source-tag.sh` retired (`tests/source-tag.test.ts` had full bun parity since v0.18.4). `tests/audit-shadow.test.ts` refactored to use new helpers (-50 LOC). Eng-review surfaced 13 decisions (5 architectural, 2 code-quality, 1 testing, 1 performance, 4 cross-model with Codex, 2 follow-up TODOs); all 9 user-facing decisions chose the complete option (lake score 9/11). 771 tests passing across 27 test files. |
| 0.18.4 | 2026-04-29 | Group 1, Track 1A: bun test toolchain bootstrap + source-tag lib port. `bun test` now runs alongside bash — `package.json` declares `engines.bun >=1.0` and `scripts.test = "bun test tests/"`; `tsconfig.json` configures strict ESM + `types: ["bun"]`. `/ship` runs both suites; full set still ~65s. New `src/audit/lib/source-tag.ts` (TS port of `bin/lib/source-tag.sh`): seven pure-function string transforms (`parseSourceTag`, `normalizeTitle`, `computeDedupHash`, `routeSourceTag`, `validateTagExpression`, `extractTagFromHeading`, `extractTitleFromHeading`), `Result<T, Reason>` discriminated union, ~10× faster than bash. Bash version stays in place — Track 2A retires it once `bin/roadmap-audit` consumes the TS module. `tests/source-tag.test.ts` (118 tests / 216 expects, full coverage): 11 parse + 9 normalize + 5 hash + 7 validate + 7 extractor ports of bash assertions, 25-row table-driven routing matrix covering all 24 canonical (source, severity) tuples per `docs/source-tag-contract.md`, tightened `INJECTION_ATTEMPT` vs `MALFORMED_TAG` reason-code asserts (now requires the specific reason, not "either"), 30 byte-exact bash-parity hash fixtures. `tests/fixtures/source-tag-hash-corpus.json` (REGRESSION-CRITICAL byte-exact bash-parity oracle): 30 inputs (ASCII, em-dashes, smart quotes, CJK / Cyrillic / Arabic, trailing-metadata sentinels, edge boundaries, internal control chars `\t`/`\n`/`\r`/`\v`/`\f`) each paired with the bash `compute_dedup_hash` output. Manifest header records `bash --version`, `uname`, `LC_ALL=C`, the byte-input contract (`Buffer.from(normalized, 'utf8')` for `printf '%s'` parity), and the generator command. `scripts/regen-source-tag-corpus.sh` forces `LC_ALL=C` before sourcing `bin/lib/source-tag.sh` so corpus regeneration can't embed locale-dependent hashes. **Pre-merge regression caught:** first `normalizeTitle` collapsed `[\t\n\v\f\r ]+` to a single space, but bash `tr -s '[:space:]'` only squeezes runs of *identical* whitespace chars (`\t \t` stays as `\t \t`), and `sed` is line-based so leading/trailing trim runs per newline-separated segment, not over the whole string. Adversarial review caught the divergence: `"line1\nline2"` was hashing differently in TS vs bash. Three-part fix: (1) sentinel-strip regexes use the `m` flag and exclude `\n` from the whitespace class; (2) whitespace squeeze keys off a backref (`/([\t\n\v\f\r ])\1+/g`); (3) trailing newlines stripped from final output to mirror bash `$(...)` command-substitution. Locked in by 7 new corpus fixtures with internal control chars. CLAUDE.md `## Testing` section names both bash and bun suites explicitly. ROADMAP Track 1A marked ✓ Complete; `bunfig.toml` and `tests/helpers/run-bin.ts` were dropped during /plan-eng-review (defaults sufficed; nothing to shell out to). |
| 0.18.3 | 2026-04-29 | `docs/designs/roadmap-phases.md` — optional Phase envelope around sequential Groups that together deliver one named end-state (e.g. "all bash test scripts deleted; bun is sole runner" across Groups 1–4 of `bun-test-architecture.md`). Solves two pains: reviewers (human + LLM) over-flagging dead-code between in-progress Group PRs because they couldn't see future Groups, and MINOR version bumps being a vibes-call when multi-Group features land. Single-source affiliation: Phase H2 marker block lists `Groups: 1, 2, 3, 4`; no per-Group tag, no drift cross-check. Audit gets a new `## PHASES` section + `PHASE_INVARIANTS` check (≥2 Groups, listed Groups exist, sequentiality, no double-claim, scaffolding `test -f`, malformed-block warns, no crash). Vocab-lint gains a fourth state (`PHASE`) so the existing "phase" ban stays strict outside structured Phase declarations. Versioning is a *recommendation* surface only — `/roadmap` freshness scan suggests MINOR on phase exit and PATCH mid-Phase; no rule becomes mechanical, the human confirms at `/ship` time. Codex outside-voice during `/plan-eng-review` drove four design changes: scope reduced to single-repo (gstack-proper skills untouched), affiliation single-sourced, parked-Phase state removed entirely, bash unit tests deferred via `[design]` TODO until the bun port lands. Implementation deferred to a future Track. Reviewed via `/plan-eng-review` (verdict: ENG CLEARED + OUTSIDE VOICE CLEARED); review report embedded at the bottom of the design doc. |
| 0.18.0 | 2026-04-28 | `/roadmap` reassessment redesign. The four-op precedence chain (REVISE → FRESHNESS → CLOSURE → TRIAGE) is replaced by a single LLM-owned reassessment step that holds the whole picture in mind and proposes a plan diff covering structural changes + closures + placements. v0.17.0 kept judgment in prose but per-op scope was too narrow — per-item placement loops never zoomed out to ask "should this set of items reshape the structure?" Dogfood evidence: `/roadmap` on bolt dumped 11 inbox items into a 6-item Pre-flight serial chain mixing 3 distinct themes. Reassessment fixes this by reading everything, identifying themes via qualitative judgment (cohesive scope? coherent files? bounded estimate? — no item-count rules), and presenting clusters via AskUserQuestion. The four ops become *kinds of changes the reassessment can propose*. Closure debt (inbox items tagged `[pair-review:group=N]`) blocks ✓ Complete in the same run — Real Done means resolving it first. Hierarchical mode (themes pass → placement pass on the full picture) handles large input when the LLM judges single-pass would be sloppy. Adversarial-flagged items drive structural decisions, not just exempt batch deferral. Mid-flight Group reopening forbidden — Hotfix is the only post-ship primitive. New: structured proposal artifact at `.context/roadmap/proposal-{ts}.md` (preview UX, parseable test surface, audit trail), tightened fast-path predicate (no rubber-stamp on stale plans — guarded against shipped-Track activity), audit-after-apply backstop, TODOS.md drain orphan check, minimal-cue prompt parsing. `bin/roadmap-place` deleted (per-item ranking no longer on critical path); `bin/roadmap-revise` slimmed to `split-track` only (`defer-task` removed — direct edits suffice). 3 new ROADMAP_VERBATIM_BLOCKS in `scripts/test-skill-protocols.sh` (125 assertions). 8 roadmap-place tests removed from `scripts/test-roadmap-audit.sh`. Plan reviewed via `/plan-eng-review` with codex outside-voice (16 challenges; 13 incorporated, 3 surfaced as cross-model tensions and resolved). Design at `docs/archive/roadmap-reassessment.md`. |
| 0.17.2 | 2026-04-28 | Review-skill TODO framing — kill tunnel vision. `/full-review` and `/pair-review` now write TODOs in hedged language so future implementers re-investigate before fixing instead of treating the original write as gospel. Two failure modes addressed at the writer side (where the fix is cheap): (1) parked bugs from `/pair-review` getting fixed weeks later from a one-line description with no repro path — implementer fixes the written symptom rather than the actual bug; (2) `/full-review` reviewer agents emitting speculative fixes that read as prescriptions and grow the codebase on every review (agents naturally suggest additive fixes — new validation, helpers, conformance). `/pair-review` park flow captures `Symptom:` + numbered `Repro:` steps with a graceful one-nudge fallback (`Repro: not reliably reproducible — verify before fixing`); both fields plus a "re-verify before implementing a fix" line propagate to TODOS.md. `parked-bugs.md` schema switches `Description:` → `Symptom:` + `Repro:`. `/full-review` reviewer-agent prompts gain "subtraction first" guidance per agent's bias (reviewer: prefer removing the violating caller; hygiene: delete duplicates before extracting helpers; consistency-auditor: consider deleting deviating modules before forcing conformance), and frame output as starting points for investigation rather than prescriptions. Agent output field renamed `FIX:` → `HYPOTHESIS:`; TODOS template renames `**Proposed fix:**` → `**Hypothesis (untested):**` (with explicit "re-investigate before implementing" trailer) and `**Why:**` → `**Description:**` (Why reads as verified causation alongside hedged hypothesis; Description is tonally neutral and matches the agent's actual DESCRIPTION field). `docs/source-tag-contract.md` registers `Symptom`, `Repro`, `Description`, `Hypothesis (untested)` as recommended attribute bullets with per-source guidance: observation-source skills (`pair-review`, `investigate`) use `Symptom:` + `Repro:`; reviewer-source skills (`full-review`, `review`) use `Description:` + `Hypothesis (untested):`; manual / `/ship` entries continue with `Why:`. New "Speculation framing" section documents the rationale — the framing is the fix, field-name parsing is unchanged. Legacy `Why:` and `Proposed fix:` field names remain valid on read (no parser inspects field names), so old TODOs.md content stays renderable. All 7 test suites pass: 547+ assertions across `test-roadmap-audit` (216), `test-skill-protocols` (122), `test-test-plan` (61), `test-update` (59), `test-source-tag` (46), `test-test-plan-e2e` (43), and `test-test-plan-extractor`. Field-name renames touch only markdown writer templates and contract documentation; parsers and audit logic are unchanged so no test fixture updates were required. |
| 0.17.1 | 2026-04-28 | `bin/roadmap-audit` dogfood-driven fixes from bolt. **Fixed:** `_size_split_suggestion` malformed sed corrupted cluster state — line 2018 used `sed "s\|${key}\|${existing_count}\|${key}\|${new_count}\|"` where `\|` was both the `s` delimiter and embedded in the data, so sed parsed everything after the first `${existing_count}` as garbage flags and errored with `unknown option to 's'`. Under `set -euo pipefail` the failed command-substitution silently returned empty, blanking `clusters` on every duplicate-key hit. Effect: oversized tracks with multiple path clusters never emitted the `Split suggestion for NX:` line because `big_count` could never reach 2. Replaced with a single-pass shell loop using literal-string equality. Also fixed neighboring `grep -oE` lookup fragility — regex metachars in path keys like `(misc)` would misbehave. **Changed (FRESHNESS scope expansion):** `git_inferred_freshness` now relaxes the 2-commit floor to 1 when the commit message references the enclosing `Track NX` (case-insensitive, dot in `7A.1`-style IDs escaped). The original 2-commit threshold filtered out the most common ship pattern: a whole Track landing in a single bundled PR shows as 1 commit on each touched file. Surfaced dogfooding `/roadmap` on bolt: Track 7A's bridge-hygiene sweep landed in commit `ceb1593` on `compose-editor.js`, but the freshness scan reported nothing because there was only one commit since intro. The Track-ID match is high-precision (commit messages that name a Track are almost always shipping that Track), so the false-positive guard isn't needed. **Added (in-place Track completion marker):** `### Track NX: Name ✓ Complete` is now a recognized Track-completion path, symmetric with the long-standing `## Group N: Name ✓ Complete` Group convention. Parser populates a new `_COMPLETE_TRACKS` state; PARALLELISM_BUDGET (and PARALLELIZABLE_FUTURE), SIZE caps, COLLISIONS, and `max_tracks_per_group` all subtract completed Tracks from their counts and pairings. The collapse-to-italic FRESHNESS step is no longer the only completion path — it's now the *later* lifecycle stage (when the whole Group winds down). Surfaced dogfooding `/roadmap` on bolt: PARALLELISM_BUDGET reported 9 in-flight Tracks against a cap of 4, then suggested "collapse Tracks 12A/12B/12C/12D bodies" as the fix — that conflated a doc-hygiene op with a real concurrency-reduction op. Now: mark them ✓ Complete in place, audit reflects actual concurrency, and the LLM stops generating cosmetic-edit suggestions as budget fixes. PARALLELISM_BUDGET emits a new `COMPLETE_TRACKS:` line so the exclusion is visible. 216 passing (was 209 → 210 with the SIZE regression test → 211 with two new `scan-state: 1 commit + Track-ID match` cases → 216 with five new `track ✓ complete` cases: max_tracks_per_group exclusion, IN_FLIGHT_TRACKS subtraction, COMPLETE_TRACKS line emission, SIZE-cap silent skip, COLLISIONS pairing skip). |
| 0.17.0 | 2026-04-28 | `/roadmap` signal-vs-verdict redesign + dogfood-driven framework fixes. The 1390-line skill is now 510 lines. **Signal-vs-verdict boundary:** helpers (`bin/roadmap-audit`, `bin/roadmap-place`, `bin/roadmap-route`, `bin/roadmap-revise`) emit raw signals only; skill prose composes ops and owns judgment. The previous design pulled too much decision logic into rigid bash — a rule-table emit fed prose that pretended to be neutral but was actually constrained by what the helper had already classified. `bin/roadmap-audit --scan-state` emits structured signal JSON (`unprocessed_count`, `in_flight_groups`, `origin_total`, `staleness_fail`, `git_inferred_freshness`, `has_zero_open_group` + intent flags). Skill prose composes the ops list (REVISE → FRESHNESS → CLOSURE → TRIAGE) using a 4-row markdown rule table that's visible, overrideable, and easy to change at runtime when the LLM has context the helper doesn't. `bin/roadmap-place` rewritten to emit ranked candidates with a `needs_judgment` flag — three patterns: 1 candidate / unambiguous → use directly; 1 candidate / `needs_judgment=1` → sanity-check on-topic-ness; 2+ candidates → judgment-required (typically origin-shipped + non-critical). Prose decides via semantic file-overlap reading instead of literal string equality (the v0.16.x version couldn't see that `components/auth/LoginForm.tsx` overlaps `ui/auth/**`). Track-ref regex made case-insensitive + normalized in `--scan-state` intent parsing — "track 2a" now matches and emits `track_ref=2A`. **Dogfood-driven framework fixes:** `route_source_tag` library function in `bin/lib/source-tag.sh` — single source of truth for the source-default routing matrix (`KEEP\|KILL\|PROMPT`); `bin/roadmap-route` is now a thin CLI wrapper around it (eliminates the previous duplication where the matrix lived in both `docs/source-tag-contract.md` and the helper's `case "$SOURCE" in` block). `signals.git_inferred_freshness` in `--scan-state` output — counts active ROADMAP.md tasks where 2+ commits landed on referenced files since the task was introduced (catches the common "shipped without updating ROADMAP.md" case that the explicit `STALENESS` check missed); the dispatcher rules now OR `staleness_fail` with `git_inferred_freshness >= 1` to trigger the FRESHNESS op (intentionally a coarse trigger; the per-item user-confirmation gate filters precisely). `[review]` registered as a source tag for the `/review` skill (pre-landing adversarial review by Claude subagent + codex); default KEEP, like `full-review:necessary`; optional `severity=critical\|necessary\|nice-to-have\|edge-case` mirrors the `full-review` taxonomy. **Skill prose convention clarifications:** Track-or-Pre-flight completion convention made explicit in the FRESHNESS op — Pre-flight is structurally a Track-equivalent within a Group, so the same collapse rule applies (when every bullet is done, collapse to a single italic line under the Group heading). Individual task completion (one bullet within an active Track or Pre-flight, siblings still open): delete the bullet and update parent metadata; git log + CHANGELOG/PROGRESS.md preserve the history. **Fixes:** `scripts/test-roadmap-audit.sh` test isolation flake — the "size: loc cap blocks" assertion hard-coded `=300` but read from the user's `~/.gstack-extend/config`, so anyone with `roadmap_max_loc_per_track=800` saw the test fail; tests now export `GSTACK_EXTEND_STATE_DIR` to an isolated tmp dir. Pre-flight 1 (`setup --skills-dir`) removed from ROADMAP.md (shipped in v0.16.0); FRESHNESS scan proof point. `docs/designs/roadmap-revamp-smart-dispatcher.md` archived to `docs/archive/`. 376 passing total (was 327 at v0.16.2): 46/46 source-tag (was 37, +9 for `route_source_tag` matrix coverage and `[review]` parse), 208/208 audit (was 206, +2 for `git_inferred_freshness` fires/doesn't), 122/122 protocols. |
| 0.16.2 | 2026-04-24 | `/roadmap` skill fixes — round two, full addresses of #3 and #5 from the original feedback. **#3 full fix:** intra-group `Depends on: Track NX` is now the canonical serialization signal — COLLISIONS skips pairs where one Track transitively depends on the other, STYLE_LINT stops warning on intra-group deps (valid DAG expression, not a bug). New `_TRACK_DEPS` parser state, `_track_depends_on` transitive-closure walker, and `_detect_track_dep_cycles` DFS (cheap — bounded by `max_tracks_per_group`). `_serialize: true_` survives as shorthand that expands into implicit edges at parse time, so downstream logic is annotation-agnostic. Replaces v0.16.1's opt-in-only escape hatch with full DAG semantics: authors can write three `cli.py` Tracks with pair-wise `Depends on: Track 1A` / `Depends on: Track 1B` and the audit accepts it without needing a Group-level annotation. Cycles (`1A Depends on 1B`, `1B Depends on 1A`) surface in STYLE_LINT with the offending path rendered (`1A → 1B → 1A`). **#5 full fix (meta-level, not just regex):** the deeper bug wasn't that VOCAB_LINT bans "cluster" — it was that VOCAB_LINT emitted `STATUS: fail` for style issues, making the skill treat nitpicks as indistinguishable from real correctness bugs (missing docs, malformed headings, cycles) and leading CC to rewrite prose to conform rather than overriding obvious false positives like "items cluster around X" (verb, not nominal synonym for Group). VOCAB_LINT now emits `STATUS: warn` — advisory. Severity rollup in `skills/roadmap.md` updates: VOCAB_LINT moved from the blocker list (`<N>`) to the advisory list (`<M>`). New "Advisory vs blocking" section under "Interpreting audit findings" documents that advisory findings can be overridden with a one-sentence rationale in the commit message or PR description; the run is `DONE_WITH_CONCERNS`, not `BLOCKED`. Closes the meta-issue — the skill now clearly communicates that style checks are not hard gates. Rule 3 in the skill-doc rewrites as well: "prefer parallel-safe Tracks; declare serialization explicitly when needed" — documents both per-pair and `_serialize: true_` options side-by-side, notes that cycles are detected, and links to the updated STYLE_LINT semantics. `scripts/test-roadmap-audit.sh` grows 182 → 185 assertions: v0.16.1's `serialize-baseline` fixture (semantically wrong under new rules — asserted COLLISIONS fail on a valid DAG) rewritten into `dag-no-deps-collides`, `dag-direct-dep`, `dag-transitive`, `dag-serialize-shorthand`, `dag-cycle`. The `style_lint: same-Group Depends on warns` test flips to `style_lint: intra-Group Depends on is valid DAG (no warn)`. New `vocab_lint: violations emit advisory (warn), not fail` test locks in the severity demotion. All 185 pass. |
| 0.16.1 | 2026-04-24 | `/roadmap` skill fixes from post-run feedback. **Correctness bug:** compact bold-form entries (`- **[tag] Title** — body`) were silently ignored — an 11-item inbox written this way would have reported "0 unprocessed" and exited with no action. UNPROCESSED now counts the form separately (`COMPACT_BULLETS: N`) and TODO_FORMAT flags each as `MALFORMED_HEADING: compact bold-form entry ... — rewrite as '### [tag] Title'` so `STATUS: found` wins and the skill can't early-exit "empty". **`_serialize: true_` escape hatch:** cohesive batches of Tracks on one monolithic file (the 3-track `cli.py` conflict-UX batch that was forced into a 5→6→7 single-Track chain only to satisfy file-granular COLLISIONS) can now annotate the Group header to opt into in-order Track execution. COLLISIONS skips pairwise checks for serialized Groups (surfaced via `SERIALIZED_GROUPS:` so the skip is visible). STYLE_LINT permits intra-group `Depends on: Track NX` for serialized Groups. `max_tracks_per_group` cap still applies. **pyproject.toml as version source:** new `read_current_version` / `read_pyproject_version` helpers; VERSION/TAXONOMY/ARCHIVE_CANDIDATES/STALENESS all fall back to `[project] version = "..."` (or `[tool.poetry] version`) when no VERSION file exists. VERSION emits `SOURCE: VERSION` or `SOURCE: pyproject.toml`. **Depends on: trailing prose tolerance:** `_Depends on: Group 5 (Auto-command) landing first before anything else_` is accepted — parser captures the leading `Group N (Name)` for identity + anchor, treats trailing clauses as commentary (previously dropped the whole annotation as unparseable). **Scrutiny gate batched mode:** inboxes ≥ 7 items use a single AskUserQuestion with the full recommendation table + "Approve all" / override path (parses `"3 keep, 7 kill, 9 defer"` free-form syntax); one-by-one stays the default ≤ 6. **Lightweight fast-path** when audit clean + inbox ≤ 3 + no closure debt + mode=triage/update: skip structural-assessment + closure-dashboard ceremony, go straight to scrutiny + drain + commit. Two self-critique nudges added to the skill: preserve existing completion-style formatting conventions (don't silently rewrite to canonical); adversarial-flagged items (Codex, `full-review:critical\|necessary`, `investigate` with incident trace) can't be batch-deferred — must surface individually. `scripts/test-roadmap-audit.sh` grows from 170 to 182 assertions: compact-bullet (3), pyproject version source (5), Depends on: trailing prose (2), `_serialize: true_` (4). All 182 pass. Skip-scoped: intentionally dropped the "cluster" verb-form vocab-lint exception (too narrow to be worth carrying) and rejected a `--quick` subcommand in favor of the auto-detected fast-path (no new mode to remember). |
| 0.16.0 | 2026-04-24 | First PR of Group 1 Install Pipeline. `setup --skills-dir <path>` flag enables per-project install locations instead of the hardcoded `~/.claude/skills/`. Arg parser rewritten from single-arg if-check into while-loop `case` for clean handling of `--skills-dir <path>`, `--uninstall`, and their combinations in any order. Strict arg validation: rejects missing values, flag-like values (e.g. `--skills-dir --uninstall`), and relative paths — all under a mocked `$HOME` so a parse regression cannot touch the real `~/.claude/skills`. Known-limitation stderr warning fires on non-default installs so silent-no-op on helper calls doesn't surprise users. Known limitation: skill preambles still hardcode `readlink ~/.claude/skills/{name}/SKILL.md`, so `--skills-dir` ships the foundation but preamble path resolution no-ops silently on non-default installs until Pre-flight 2 lands the probe-pattern fix. 15 new test assertions in `scripts/test-update.sh` (59 total, was 44) covering custom-dir install, defense-in-depth isolation from default dir, arg-rejection cases, warning-fires-on-custom-not-default, uninstall across all 5 skills, reversed flag order, and paths with spaces. Design locked via `/plan-eng-review` with codex outside-voice pivoting scope from sed-inject/generated-copy deploy to gstack-core's probe pattern (matches the reference implementation, preserves symlink live-edit, simpler uninstall semantics). See ROADMAP.md for full Group 1 scope and ordering `1 → 2 → Track 1A → 3 → 4`. |
| 0.15.2 | 2026-04-25 | Drift-proof shared graft enforcement. `scripts/test-skill-protocols.sh` gains a `REQUIRED_VERBATIM_BLOCKS` assertion block (102 → 122 assertions) that asserts four cross-skill protocol fragments — Completion Status Protocol enum, Escalation opener, Escalation format, Confusion Protocol head — are byte-identical across all 5 skills. Edits become a deliberate two-step: change canonical text in the test script, watch tests fail, propagate to all 5. Drift detection verified via manual mutation. `<!-- SHARED:<block-name> -->` HTML marker pairs (4 per skill × 5 = 20 pairs) bracket the shared fragments: invisible to LLMs reading prose, explicit signal to humans maintaining shared content, pre-stages the deferred `SKILL.md.tmpl` TODO for trivial mechanical extraction later. Small duplication-only trims to `/full-review` (removed `## Error Handling` summary, -36 lines; verified inline-covered in Phases), `/review-apparatus` (removed "Ambiguity rule" pointer), `/pair-review` (removed duplicated State path restatements; fixed duplicate `---`), `/roadmap` (removed duplicate subcommand auto-detect footer), `/test-plan` (fixed duplicate `groups/` heading in state diagram). `docs/TODOS.md` rebalanced: Codex-specific gates (description ≤ 1024 chars, env-var preamble) moved back to the Codex host TODO; Simplification TODO codifies scope discipline (only obvious dups), shared-fragment canonicalization strategy, execution order (Lane A serial first then Lanes B-F parallel), and explicit regression-surface trade. Net skill-file change -11 lines; real value is the 20 new verbatim-block assertions that make future simplification and the deferred `SKILL.md.tmpl` work mechanical. |
| 0.15.1 | 2026-04-24 | `/roadmap` scrutiny + closure culture upgrade. New source-tag contract (`docs/source-tag-contract.md`) + parser library (`bin/lib/source-tag.sh`) define canonical rich-format entries (`### [source:key=val] Title` + child bullets) for all TODOS.md writers. Audit gains `IN_FLIGHT_GROUPS` (topo-sort over DAG, not doc order), `ORIGIN_STATS` (per-Group open-origin counts), and `TODO_FORMAT` validator (rejects malformed headings, unknown sources, injection attempts). `check_unprocessed` counts `### ` headings, not `- ` bullets (was miscounting child bullets as items). `/roadmap` Step 2a scrutiny gate now runs in triage/update modes with per-source default recommendations (full-review:edge-case → SUGGEST KILL, observed bugs → KEEP). Closure bias in Step 3b/3c: origin-tagged items route back to the Group that surfaced them, not to Future. Reopen rule: PROMPT with smart default when origin-tagged bug arrives at a `✓ Complete` Group. Dedup pre-pass in Step 2a uses `hash(normalize_title)` (cross-source collapse; source preserved on kept entry; JSONL log at `.context/roadmap/dedupe-log.jsonl`). Closure debt dashboard + auto-suggest closure walk at top of /roadmap output. **Stable Group IDs**: completed Groups stay `## Group N: Name ✓ Complete` in place, no renumbering — `skills/roadmap.md:797-811` rewritten to reverse prior remove-and-renumber behavior (load-bearing for origin tag resolution forever). Audit's `_COMPLETE_GROUPS` list drives exclusion from active counts in `STRUCTURAL_FITNESS`/`IN_FLIGHT_GROUPS`/`ORIGIN_STATS`; `TASK_LIST` keeps complete Groups as ground truth with new `complete=1` flag. `/full-review` severity taxonomy expanded to `critical|necessary|nice-to-have|edge-case`; edge-case findings dropped at source (never written to TODOS.md). `/pair-review` gains defer-nudge at "Send to TODOS.md" branch + writes rich-format origin tags `[pair-review:group=<slug>,item=<idx>]`. `/review-apparatus` + `/test-plan` writers also emit rich format. New `scripts/test-source-tag.sh` (33 assertions: grammar, normalization, dedup hash, validator reasons, extractors). `scripts/test-roadmap-audit.sh` extended from 141 to 160 assertions (new: complete_groups, in_flight_topo, origin_stats, todo_format). 443 total assertions across 6 suites, 0 failures. Scope grew 4 → 8 files + 11 → 16 items after /plan-ceo-review + /plan-eng-review + two rounds of Codex outside-voice (Codex caught: TODOS.md format parser bug miscounting 16 items as 5, DAG-aware in-flight topology, cross-source dedup key correction, reopen semantics gap, review-apparatus+test-plan writer blast radius, completed-Group renumber hazard). |
| 0.15.0 | 2026-04-21 | New `/test-plan` skill: Group-scoped batched test-plan generator that composes with `/pair-review` as the execution engine. Reads a roadmap Group's Tracks, harvests CEO/eng/design review docs for those branches, auto-detects any per-Track pair-review artifacts and consumes 5 categories (skip PASSED, surface SKIPPED/DEFERRED/FIXED-as-regression-when-build-differs, carry PARKED, flag FAILED+FIXED as regression candidates when integrated build differs from verified build), extracts test items via LLM prompt with strict JSON contract, classifies automated/manual via conservative heuristic (ambiguous defaults to manual), writes `-test-plan-batch-*.md` to `~/.gstack/projects/<slug>/` matching `/qa-only`'s discovery glob, populates `.context/pair-review/session.yaml` + `groups/<group>.md`, archives prior groups files on re-run (strict handoff, no merge), and drops into `/pair-review`'s Phase 2 execution loop. Enforces single integrated deploy target (Track branches are provenance-only). Stable item IDs: `sha256(branch\|doc\|section\|normalized-desc)[:8]`. v1 scope: `run` + `status` subcommands; `seed`/`retro`/per-item-automation deferred to v2. Explicit Group→branch manifest at `~/.gstack/projects/<slug>/groups/<group>/manifest.yaml`. New `docs/designs/test-plan-artifact-contract.md` owns the file-format spec (schema 1). `scripts/test-test-plan.sh` (58 assertions: slugify, item IDs, path construction, archive, state-write failure guard, classification signals, subcommands, provenance tags, consume categories, integrated-build guard). `scripts/test-test-plan-extractor.sh` (21 assertions + golden-set scoring harness against 2 real design docs with >=70% tolerant-match threshold). `scripts/test-test-plan-e2e.sh` (43 assertions: fixture repo with ROADMAP Group + Tracks, review-doc discovery, pair-review consumption across all 5 categories, Phase 7 archive+write, Phase 6 batch-plan shape, qa-only glob compatibility, TODOS.md append, idempotence). `scripts/test-skill-protocols.sh` extended to cover 5 skills; `scripts/test-update.sh` updated for new symlink + "Installed 5 skills". README and CLAUDE.md routing rule added. Scope revisions during /plan-eng-review: v1 cut from 4 subcommands to 2 (codex caught), single-deploy-target execution model (codex caught cross-branch execution gap), explicit Group→branch manifest (codex caught invented mapping), stable item IDs added (codex caught missing identity for future retro). Design doc with full revision trail at `~/.gstack/projects/kbitz-gstack-extend/kb-kbitz-test-plan-skill-design-20260421-115221.md`. |
| 0.14.0 | 2026-04-20 | `/roadmap` gains optional **Group-level `_Depends on:_`** annotations so parallel workstreams can express a DAG instead of a single linear chain. Default (no annotation) = depends on the preceding Group (backward compatible). `_Depends on: none_` or `—` marks a Group as no-deps. `bin/roadmap-audit` adds a new `## GROUP_DEPS` section: parses annotations, builds the DAG, runs Kahn's cycle detection, validates forward refs, emits `STALE_DEPS` warn on drifted name anchors, and always renders a topologically-ordered adjacency list in the Execution Map. New `STYLE_LINT` rule warns on redundant-backwards-adjacent deps. Renumber pass (Step 3.5d) extended to rewrite all Group-level `_Depends on: Group N_` refs + refresh name anchors when a Group is deleted. `scripts/test-roadmap-audit.sh` gains 18 new test cases (141 total, 0 failures). Scope-rejected after codex review: Streams as a first-class primitive, `/pair-review --stream/--groups` flag, swim-lane ASCII render — adjacency list carries the information without the failure surface. |
| 0.13.0 | 2026-04-18 | New `/review-apparatus` skill: audits a project's existing testing and debugging apparatus (scripts, `bin/` tools, Makefile targets, dev endpoints, logging, staging configs, test infra) and proposes lightweight bolt-on additions where a small helper would make CC-assisted verification or debugging materially easier. Approved proposals land in `docs/TODOS.md` as `[review-apparatus]` items for `/roadmap` to classify. `/roadmap` source-tag signal list updated to recognize `[review-apparatus]`. `scripts/test-skill-protocols.sh` extended 62 → 82 assertions; `scripts/test-update.sh` updated for the new skill symlink and install count (3 → 4). README gains a table entry and full skill section. |
| 0.12.0 | 2026-04-18 | GSTACK REVIEW REPORT table rendering added to all three skills. `/full-review` prepends it to `.context/full-review/report.md` + emits in chat. `/roadmap` leads every audit summary with it. `/pair-review` emits a per-group mini-table at each group checkpoint AND a session-done rollup (one row per group). Verdict line maps from v0.11 Completion Status Protocol enum. `scripts/test-skill-protocols.sh` extended from 36 to 62 assertions. Third and final PR in the gstack-parity sequence. |
| 0.11.0 | 2026-04-18 | Completion Status Protocol, Escalation format, and Confusion Protocol grafted into `/pair-review`, `/roadmap`, and `/full-review`. Each skill now rolls up to `DONE` / `DONE_WITH_CONCERNS` / `BLOCKED` / `NEEDS_CONTEXT` with per-skill rules tied to its existing phase/item vocabulary. No behavior change to the skills' main flows. New `scripts/test-skill-protocols.sh` gates 36 grep assertions across the three skills (36/36 pass). Second of three PRs in the gstack-parity sequence. |
| 0.10.0 | 2026-04-18 | Dropped `/browse-native` beta and all supporting infrastructure (`docs/debug-infrastructure-guide.md`, `scripts/validate.sh`, `--with-native` setup flag). Zero known users, ongoing maintenance cost for no shipping value. README skill table now lists only the three stable skills. `scripts/test-update.sh` regression-tests that `--with-native` is rejected and that uninstall still cleans up legacy `browse-native` symlinks for pre-0.10 installs. First of three PRs in the gstack-parity sequence (see `~/.gstack/projects/kbitz-gstack-extend/kb-kbitz-gstack-patterns-design-20260418-105937.md`). |

## Phase 1: Core Adapter (retired)

Built the `/browse-native` skill against real macOS apps. Beta never left beta; retired in v0.10.0.

| Version | Date | Summary |
|---------|------|---------|
| 0.9.0 | 2026-04-18 | /roadmap size + collision enforcement. New `_touches:_` metadata line makes file sets explicit; audit emits `## SIZE` (per-track caps: 5 tasks, 300 LOC, 8 files, all tunable via `bin/config`), `## COLLISIONS` (intra-Group pairwise, classified SHARED_INFRA vs PARALLEL using `docs/shared-infra.txt` glob-matched patterns), `## STYLE_LINT` (warns on same-Group `Depends on:`), and `## SIZE_LABEL_MISMATCH` (warns on effort vs lines-hint divergence). New `bin/lib/effort.sh` for LOC mapping + config fallback with numeric validation. Legacy tracks tolerated via skip-legacy fallback + migration prompt. Own ROADMAP.md migrated (Track 1B deleted, tasks promoted to Pre-flight). 156 tests total (111 audit + 45 update), 0 failures. |
| 0.8.10 | 2026-04-15 | Release pipeline fix: tightened update-check version regex, auto-tag GitHub Action, 4-digit version tests, README versioning section (MICRO semantics), gist bridge comment. Fixed bash 3 crash in roadmap-audit (associative arrays), fixed stale setup test expectations. 128 tests, 0 failures. |
| 0.8.9.0 | 2026-04-14 | Roadmap audit accepts 4-digit versions (X.Y.Z.W). MICRO segment for doc-only/config bumps. Removed stale design docs, trimmed CLAUDE.md, refreshed roadmap (Track 2A done, added Track 1B: Roadmap Onboarding). |
| 0.8.8.1 | 2026-04-14 | Public-ready cleanup: MIT license, gist-to-raw-GitHub version check migration, removed archived design docs with internal references, anonymized app name across docs and skill, SSH→HTTPS clone URL. |
| 0.8.8 | 2026-04-13 | /roadmap triage mode now runs the freshness scan (Step 3.5) before classifying items into groups. Previously, triage slotted new items into potentially-complete groups because the scan was gated to update mode only. Stale/completed tasks are now always cleaned before new items get placed. Early exit no longer skips the freshness check. |
| 0.8.7 | 2026-04-12 | /pair-review latency optimization: lookahead display (next item preview inline with current), batch mode (3 items at once with "All pass"), PASS/SKIP fast path (cached lookahead + parallel state writes). Reduces perceived wait and actual round-trips. |
| 0.8.4 | 2026-04-07 | /roadmap doc discovery: scans all .md files for scattered TODOs, extracts actionable items with one-by-one triage, deduplicates against existing TODOS.md/ROADMAP.md, merges with [discovered:filepath] provenance tags, and offers doc reclassification (rewrite as spec, delete TODO sections, or leave with drift detection). Hybrid architecture: deterministic bash audit for discovery, LLM for semantic extraction and dedup. 17 new tests (65 total). |
| 0.8.1 | 2026-04-06 | /roadmap phase triage step: keep/kill + phase assignment before Group/Track placement. Items triaged to current phase or deferred to Future. Version history audit: removed invalid 0.4.2 (doc-only rename folded into 0.4.1), added missing 0.8.0 CHANGELOG entry. |
| 0.8.0 | 2026-04-06 | New /full-review skill: weekly codebase review pipeline with 3 specialized agents (reviewer, hygiene, consistency-auditor) dispatched in parallel, root-cause clustering for triage UX, human approve/reject/defer per cluster, approved findings written to TODOS.md as `[full-review]` source-tagged items. Dedup against ROADMAP.md prevents re-flagging tracked issues. Incremental state checkpointing for resume support. Designed to feed into /roadmap for execution topology. |
| 0.7.0 | 2026-04-06 | New /roadmap skill: deterministic audit script (8 checks, 28 tests) + skill prompt for doc restructuring into Groups > Tracks > Tasks. TODOS.md/ROADMAP.md split (inbox vs execution plan). Two modes: overhaul (first run) and triage (process unprocessed items). /pair-review writes to TODOS.md Unprocessed section with source tags. Shared semver lib extracted. |
| 0.6.3 | 2026-04-06 | browse-native gated as opt-in beta: `./setup` only installs stable skills by default, `--with-native` flag for beta. Unknown flags now rejected. README updated with maturity status. Test suite expanded to 24 tests. |
| 0.6.2 | 2026-04-05 | Bug fixes: `update-run` safe branch handling, preamble update-check guard works from any clone location. Smart next-step suggestion at pair-review completion (nudges `/review` before `/ship` for non-trivial changes). New test suite `scripts/test-update.sh` (17 tests). |
| 0.6.1 | 2026-04-05 | /pair-review UX fixes: standardized all prompts to AskUserQuestion with explicit options (eliminates inconsistent question styles across workspaces), added Conductor visibility awareness with action receipt pattern (important status updates always visible in final message). |
| 0.6.0 | 2026-04-05 | Auto-update system: `bin/update-check` (gist-based remote VERSION, pure bash semver, caching, snooze) + `bin/update-run` (git pull + setup) + `bin/config` (key=value). Inline upgrade flow in skill preambles. GitHub Action syncs VERSION to gist. Global-install only. State in `~/.gstack-extend/`. |
| 0.5.0 | 2026-04-05 | Bug parking for /pair-review: park unrelated bugs during testing, triage at group completion (fix now / defer to TODOS.md / keep parked), post-testing fix queue (Phase 2.5). Avoids git add -u pollution by deferring TODOS.md writes to group boundaries. Design doc updated. |
| 0.4.1 | 2026-04-04 | Added `setup` script for skill symlink installation. Handles both global (~/.claude/skills/) and per-project installs, with --uninstall support. README updated with proper install instructions. Renamed project from gstack-native to gstack-extend. |
| 0.4.0 | 2026-04-04 | New /pair-review skill: pair testing session manager with deploy discovery, grouped test plans from diffs, test-fix-retest loop with group-level checkpoints, and cross-machine resume. General-purpose (web, native, CLI). Design doc: docs/designs/pair-review.md |
| 0.3.1 | 2026-04-04 | Implementation guide for adding debug infrastructure to new apps (docs/debug-infrastructure-guide.md). Skill now detects missing infrastructure and guides users to add it before proceeding in degraded mode. |
| 0.3.0 | 2026-04-04 | Replaced Peekaboo with inside-out debug pattern. App instruments itself (screenshots via ScreenCaptureKit, layout probes, state dumps). Agent communicates via filesystem triggers + osascript. Three-tier degraded mode (full/partial/screenshot-only). New validation gates. Design doc: docs/archive/inside-out-debugging.md |
| 0.2.0 | 2026-03-27 | Skill rewrite from hands-on feedback: reframed as focused interaction tool (not autonomous QA), mandatory target requirement before starting, capability probe at session start, keyboard-first mode for sparse AX trees (SwiftUI), --app fallback when --window-id fails, action batching, adaptive waits, error recovery patterns, honest limitations section, replaced QA checklist with scoped reporting. |
| 0.1.2 | 2026-03-25 | Gate 2 rewrite: element-ID targeting, state verification, button click by ID. Spec aligned with implementation. 20-step proof deferred to TODOS. |
| 0.1.1 | 2026-03-25 | Simplified to 3 gates, switched default to TextEdit, added close-button cleanup, fixed hotkey syntax |
| 0.1.0 | 2026-03-24 | Initial /browse-native skill, validation gates (4 gates), design doc, TODOs |

## Roadmap

- **Phase 1 (Groups 1–4): Bun Test Migration** — ✓ Complete (v0.18.3 → v0.18.11.0). `bun test` is the sole entry point; `bin/roadmap-audit` is a 7-line shim invoking `src/audit/cli.ts`; touchfiles diff selection + audit-compliance + skill prose corpus shipped. Suite 113s → 32s; audit snapshots 124s → 7.3s.
- **Group 5: Install Pipeline** — Single Track 5A "Install pipeline polish" with 5 tasks (preamble probe, layout scaffolding, doc type detection, update-run dir propagation, setup symlink hardening).
- **Group 6: Distribution Infrastructure** — Track 6A: auto-detect major version boundary (waits for an external 0.x → 1.x bump).
- **Group 7: Audit Polish** — Track 7A with 3 tasks: direct phase-check tests, STALENESS rename, FRESHNESS scan extends to TODOS.md.
- **Groups 8 → 12: Skill ecosystem polish chain** — 5 sequentially-dependent Groups, each shipping one Track: 8A git commit failure handling → 9A `/gstack-extend-upgrade` skill → 10A telemetry parity → 11A `/claude-md-cleanup` skill → 12A skill-file simplification + SKILL.md.tmpl.
- **Future** — Eval persistence + reader (waits for a producer Track), multi-agent test orchestration, shared-infra auto-detect, Codex host support, audit fail-taxonomy calibration, etc.
